<Page x:Class="ProjectInfinity.Menu.View.MenuPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:menu="clr-namespace:ProjectInfinity.Menu;assembly=ProjectInfinity.Implementation" 
    Title="MenuPage" Background="{StaticResource backGroundBrush}" 
    >
  <Page.Resources>
    <HierarchicalDataTemplate DataType="{x:Type menu:Menu}" ItemsSource ="{Binding Path=Items}">
      <TextBlock Text="{Binding Path=Text}" />
    </HierarchicalDataTemplate>
    <DataTemplate DataType="{x:Type menu:PluginItem}" >
      <WrapPanel>
        <TextBlock Text="{Binding Path=Text}" FontSize="20"  MouseDown ="Clicked"/>
        <TextBlock Text=" (" FontSize="20" />
        <TextBlock Text="{Binding Path=Description}" FontSize="20" />
        <TextBlock Text=")" FontSize="20"/>
      </WrapPanel>
    </DataTemplate>

  </Page.Resources>
  <StackPanel>
    <TreeView Name ="trvMenu"/>
  </StackPanel>
</Page>
