using System.ComponentModel;

namespace ProjectInfinity.Windows
{
  /// <summary>
  /// Empty interface (for now) to recognise the main window.
  /// </summary>
  public interface IMainWindow
  {
   // event CancelEventHandler Closing;
  }
}